class PhysicalObject:

    def __init__(self, name, feature, featuretype):
        """
        Save an object's name together with a feature and a feature type
        """
        self.name = name
        self.feature = feature
        self.featuretype = featuretype
